import 'package:flutter/material.dart';

class NewAndHotPage extends StatelessWidget {
  const NewAndHotPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('NewAndHotPage'),
      ),
    );
  }
}
